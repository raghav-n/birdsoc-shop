name: Django Oscar CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: ["3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests
      env:
        SECRET_KEY: ci-test-secret-key
        JWT_SECRET: ci-test-jwt-secret
      run: |
        python manage.py test apps.api.tests -v 2

  deploy:
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install deployment dependencies
      run: |
        python -m pip install --upgrade pip
        pip install gunicorn

    # Example deployment step - modify to suit your needs
    - name: Deploy to production
      # Uncomment and customize these lines for your deployment method
      # For example, if using SSH deployment:
      # uses: appleboy/ssh-action@master
      # with:
      #   host: ${{ secrets.HOST }}
      #   username: ${{ secrets.USERNAME }}
      #   key: ${{ secrets.SSH_KEY }}
      #   script: |
      #     cd /path/to/project
      #     git pull
      #     source venv/bin/activate
      #     pip install -r requirements.txt
      #     python manage.py migrate
      #     systemctl restart your-service
      run: |
        echo "Add your deployment commands here"
        # This is just a placeholder, replace with actual deployment logic
